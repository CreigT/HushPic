# HushPic currently performs image processing entirely in the browser.
# No API key is required for the free client-side application.
# Add production service variables here only when a real backend integration is introduced.
